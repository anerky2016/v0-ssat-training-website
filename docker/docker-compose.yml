version: '3.8'

services:
  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    # Uncomment and create .env file if needed
    # env_file:
    #   - ../.env.local
    restart: unless-stopped
    # Uncomment to add volume for logs
    # volumes:
    #   - ./logs:/app/logs
